{
  "name": "derby-stats",
  "version": "1.0.0",
  "description": "",
  "main": "src/server/server.js",
  "type": "module",
  "scripts": {
    "build:client": "cd src/ui && npm run build && cd ../..",
    "dev:server": "tsc && nodemon ./build/server/server.js",
    "dev": "npm run build:client && npm run copy-ui-build && npm run dev:server",
    "copy-ui-build": "mkdir -p build/ui && cp -r src/ui/dist/ build/ui",
    "copy-ui-dist": "mkdir -p dist/ui && cp -r src/ui/dist/ dist/ui",
    "test": "echo \"Error: no test specified\" && exit 1",
    "package": "npm run build:client && npm run copy-ui-dist && tsc && pkg --out-path dist ./build/server/server.js"
  },
  "author": "",
  "license": "GPL-3.0",
  "dependencies": {
    "express": "^4.18.2",
    "express-ws": "^5.0.2",
    "ws": "^8.13.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/express-ws": "^3.0.1",
    "@types/ws": "^8.5.5",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "nodemon": "^3.0.1",
    "pkg": "^5.8.1",
    "typescript": "^5.2.2"
  }
}
